# ClaimIDX listing staging

Path B: pip install "claimidx[server]>=0.7.12" → claimidx init --agent NAME → claimidx claim --yes --channel dir-aianytime --source path-b
Optional hold: cix_bdc82291f2fbb06a
